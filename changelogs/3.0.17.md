# 3.0.17

## Remove Gas Burning Generator per-tick debug spam

- Stripped `[GasGenerator Debug] Tick` / `Fuel details` INFO logs that fired every server tick and flooded `latest.log` (and hurt TPS observability).
- Empty mixin retained only so older mixin configs remain valid.
